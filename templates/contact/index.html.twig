{% extends 'base.html.twig' %}

{% block title %}Nous contacter- My Hotel{% endblock %}

{% block content %}
<h2>Nous contacter</h2>
{% for message in app.flashes('notice') %}
<div class"alert alert-info">{{ message }}</div>
{% endfor %}

  <div class="row">
    <div class="col-md-8" >

  {{ form_start(form) }}

    {{ form_row(form.prenom) }}
    {{ form_row(form.nom) }}
    {{ form_row(form.email) }}
    {{ form_row(form.content) }}
   
 {{ form_end(form) }}
    
    </div>
  
  
  
  </div>


{% endblock %}
